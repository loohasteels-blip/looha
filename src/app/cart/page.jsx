import CartComponent from '../../components/Cart';

export const metadata = {
  title: 'Your Cart — Looha Steel',
  description: 'Review your steel order before checkout.',
};

export default function Page() {
  return <CartComponent />;
}
