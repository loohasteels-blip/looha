import PaymentPolicyComponent from '../../components/PaymentPolicy';

export const metadata = {
  title: 'Payment Policy — Looha Steel',
  description: '100% advance payment policy for steel orders.',
};

export default function Page() {
  return <PaymentPolicyComponent />;
}
