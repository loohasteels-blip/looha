import LoginComponent from '../../components/Login';

export const metadata = {
  title: 'Register — Looha Steel',
  description: 'Create your Looha account.',
};

export default function Page() {
  return <LoginComponent />;
}
